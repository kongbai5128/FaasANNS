# two_stage_search query test

默认直接请求云函数，不经过本地 VM：

```bash
./baseline/functions/Two_stage\ search/test/run_queries.sh
```

结果默认写入：

```text
baseline/functions/Two_stage_search/test/result/run_queries.csv
```
